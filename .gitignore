# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# The workspace target directory. Cargo places it at the workspace root, which
# is now the repository root — `crates/desktop/.gitignore` still covers the
# crate-local one for builds made with an explicit --target-dir.
/target/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/supabase
